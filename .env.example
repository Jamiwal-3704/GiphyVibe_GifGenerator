REACT_APP_GIPHY_API_KEY=your_giphy_api_key_here

# If you do not want to run the server proxy, set the key above and the client will
# fallback to calling GIPHY directly (note: client-side keys are exposed in bundles).
